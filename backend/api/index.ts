// Vercel serverless entry point
// This file is referenced by vercel.json and exports the Express app
// so Vercel can run it as a serverless function.
import app from '../src/app';

export default app;
